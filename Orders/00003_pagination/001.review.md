# レビュー

## 指摘事項

- [Do] `backend/src/handlers/todoHandlers.ts` の `listTodoHandler` の 404 エラーの実装について、現状のロジック (todo が 0 件の時 page=1 の指定を許容する) で問題ないので、コメントを「todo が 0 件の時 page === 1 は許容する」というシンプルなものに修正してください。
  - [Do] if 文が二つ連続していますが、どちらも `page` と `totalPage` の関係に対して 404 を返却するか否かを確認するものなので `if, else if` を使ってください。
- [Q(Done)] `frontend/app/types.ts` に `GetTodoListResponse` と `PaginationMeta` がありますが、これは API の返却値の型なので `frontend/app/api/todoApi.ts` に実装してほしいと思っているのですが、一方でプロダクトで使う型情報が一か所にまとまっていることの便利さは理解できます。TypeScript ではこのようなケースにおいて `types.ts` に型をまとめるのが一般的でしょうか？
- [Do] ToDo 一覧ページの最後のページに表示される Todo の最後の一件を削除した際、　`GET /todo?page=(最後のページ)` のリフェッチが発生し 404 エラーが返却されるのでページ情報が更新されないまま、削除済みの todo が表示され続けてしまいます。最後の要素となった場合は一つ前のページを表示するように修正してください。
  - todo が 1 しか存在しない状態でそれを削除した場合は「todo が 0 件の時 page === 1 は許容する」を利用したいので一つ前のページではなくそのまま 1 ページ目を表示するようにしてください
- [Do] `frontend/app/components/PaginationControl.tsx` の実装ですが `MUI` の `Pagination` コンポーネントを使うようにしてください。　`Orders/00003_pagination/001.order.md` に記載したページ毎のページネーションの表示例と少し異なりますが、 `<Pagination count={(ページ数)} showFirstButton showLastButton />` を使ってもらえれば概ね仕様は満たしているのでそれでお願いします。 (Order を記載する際、ちゃんと調べておらず申し訳ないです。)
- [Do] `frontend/app/types.ts` に `GetTodoListResponse` と `PaginationMeta` がありますが、これは API の返却値の型なので `frontend/app/api/todoApi.ts` に実装してほしいです。

## 注意点

- Implementation Plan, Task, Walkthrough は日本語で出力してください
- 指摘事項が `[Q]` から始まるものがあれば、すべての対応を行う前に質問に回答すること
  - その解答に応じて `[Do]` または `[Q]` から始まる指示を追記します
  - また質問に答えられた `[Q]` は `[Q(Done)]` に変更します
    - `[Q(Done)]` は質問したことをログとして残すために使用します
    - `[Q(Done)]` から始まる指示は無視してください
- 対応方法が複数考えられるものがある場合は、すべての対応を行う前に対応方法を説明し、どの方法を選択するか確認すること
  - その説明に応じて `[Do]` または `[Q]` から始まる指示を追記します
- `[Q]` から始まる指摘事項や対応方法が不明確な指摘事項がなくなったら対応を開始すること
- 指摘事項が `[Do]` から始まる場合は、指示に従い対応すること
- 指摘事項に対応する場合は1点につき1コミット作成すること
