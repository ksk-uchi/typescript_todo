{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "attach",
      "name": "Attach to Podman (tsx)",
      "port": 9229,
      "address": "localhost",
      "localRoot": "${workspaceFolder}",
      "remoteRoot": "/app", // コンテナ内のソースコード配置場所に合わせる
      "skipFiles": ["<node_internals>/**"],
      "sourceMaps": true
    }
  ]
}
